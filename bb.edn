{:paths ["src" "test"]
 :tasks
 {test {:doc "Run tasuke cljc actor boundary tests."
        :task (do
                (require 'clojure.test
                         'tasuke.murakumo-test)
                (let [run-tests (requiring-resolve 'clojure.test/run-tests)
                      r (run-tests 'tasuke.murakumo-test)]
                  (System/exit (if (zero? (+ (:fail r) (:error r))) 0 1))))}}}
